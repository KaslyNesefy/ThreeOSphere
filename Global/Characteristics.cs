public class Characteristics1
{
    //private protected byte[] _characteristicsValues = new byte[(byte)EPowers.LastPower];
    private Characteristics _powers;
    private protected byte _karma;
    public Characteristics1(Characteristics powers)
    { 
        foreach (byte power in powers)
        {
            _characteristicsValues[i] = 1;
        }
    }

    private protected virtual void InitializeCharacteristics()
    {
    }

}